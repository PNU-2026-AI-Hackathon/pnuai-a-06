# Backend

Basic FastAPI backend for the Jjigeukka project.

## Run

```bash
conda activate pnuai
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Open http://127.0.0.1:8000/docs to view the API docs.
