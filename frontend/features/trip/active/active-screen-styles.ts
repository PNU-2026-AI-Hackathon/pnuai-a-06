import { StyleSheet } from 'react-native';

// active 화면의 기본 컨테이너와 스크롤 영역 스타일입니다.
export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#ffffff',
    flex: 1,
  },
  scrollContent: {
    backgroundColor: '#ffffff',
    paddingTop: 28,
  },
  inlineMessage: {
    color: '#409CB7',
    fontSize: 12,
    fontWeight: '700',
    marginTop: 10,
  },
});
